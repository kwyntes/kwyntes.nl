{
  "source": "src/index.html",
  "scripts": {
    "start": "parcel",
    "build": "parcel build && cp src/_redirects dist/"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.1.1",
    "parcel": "^2.1.1"
  }
}
